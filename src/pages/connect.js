import Image from "next/image";
import localFont from "next/font/local";
import React from 'react';

export default function connect() {
  return (
    <div class="bg-white  text-center text-black h-screen justify-center">
      <p class="p-10 xanh-mono-regular-bold">
        this is probably where we ask for the user to sign onto steam
        OR import their own data (if we get to it) (not collected probably cuz privacy concerns.
        OR WE SHOULD ASK THEM TO DO IT ON THE LANDING PAGE) </p>
    </div>
  );
};

